`social-preview.svg` is the GitHub Social Preview banner (1280x640, dark theme, scan-clean SVG).
To activate the LinkedIn/Twitter card: export to 1280x640 PNG and upload at GitHub repo Settings → General → Social preview (manual; no API).
No external font or image references — pure vector + system fonts, safe to version-control.
