`social-preview.png` is the GitHub Social Preview banner (1280x640, metadata-stripped PNG, dark low-poly node-network brain design).
To set the LinkedIn/Twitter card: upload this PNG at GitHub repo Settings → General → Social preview (manual; no API).
No EXIF GPS, artist, author, comment, or personal software tags — produced by `sips` re-encode with `xattr -c` applied.
